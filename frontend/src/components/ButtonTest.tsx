import React from "react";
import { Button } from "@mui/material";

const ButtonTest = () => {
  return <Button variant="contained" color="primary">Test Button</Button>;
};

export default ButtonTest;
