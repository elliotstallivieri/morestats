import React from "react";
import { TextField } from "@mui/material";

const SearchBar = () => {
  return (
    <TextField fullWidth placeholder="Search a champion" variant="outlined" />
  );
};

export default SearchBar;
